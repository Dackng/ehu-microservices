spring:
  profiles:
    active: heroku
---
spring:
  profiles: heroku
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://user:password@localhost:5000}/eureka/
  instance:
    hostname: ${DOMAIN_NAME}
    nonSecurePort: 80
  password: ${USER_PASSWORD:password}
  datasource:
    url: jdbc:postgresql://ec2-23-21-76-49.compute-1.amazonaws.com:5432/d5ta9o8vjuceqc
    username: hgkhjdodvpqesd
    password: a3c83002fc274a0f50d8041f4c6f00aa70844beae40ecf5fbeaaafb534fc8735
    maxActive: 10
    maxIdle: 5
    minIdle: 2
    initialSize: 5
    #removeAbandonedTimeout: 600
    removeAbandoned: true    
  jpa:
    hibernate:
      ddl-auto: none
      dialect: org.hibernate.dialect.PostgreSQLDialect
      show_sql: true
  
---
server:
  port: 8013
spring:
  profiles: development
  datasource:
    url: jdbc:postgresql://localhost:5432/catalogdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
      dialect: org.hibernate.dialect.PostgreSQLDialect
      show_sql: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/

---
spring:
  profiles: test
  application:
    name: ehu-catalog-service
  datasource:
    url: jdbc:postgresql://localhost:5432/catalogdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
      dialect: org.hibernate.dialect.PostgreSQLDialect
      show_sql: true
