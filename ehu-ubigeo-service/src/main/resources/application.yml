spring:
  profiles:
    active: heroku

---
spring:
  profiles: heroku
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://user:password@localhost:5000}/eureka/
  instance:
    hostname: ${DOMAIN_NAME}
    nonSecurePort: 80
  password: ${USER_PASSWORD:password}
  datasource:
    url: jdbc:postgresql://ec2-54-235-153-124.compute-1.amazonaws.com:5432/d97r7ru1gv11ti
    username: ynyrybozcxyccd
    password: 40bb44d2d735ca8ee3623705f1fd6e08fe7b887637a5ef3ae176b35d0156d4b7
    maxActive: 10
    maxIdle: 5
    minIdle: 2
    initialSize: 5
    #removeAbandonedTimeout: 600
    removeAbandoned: true    
  jpa:
    hibernate:
      ddl-auto: none
      dialect: org.hibernate.dialect.PostgreSQLDialect
      show_sql: true

---
server:
  port: 8015  
spring:
  profiles: development
  datasource:
    url: jdbc:postgresql://localhost:5432/ubigeodb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none 
      dialect: org.hibernate.dialect.PostgreSQLDialect
      show_sql: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/

---
spring:
  profiles: test
  application:
    name: ehu-ubigeo-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ubigeodb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none 
      dialect: org.hibernate.dialect.PostgreSQLDialect
      show_sql: true
